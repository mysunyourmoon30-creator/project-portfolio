bin/
obj/
*.user
.vs/
clone/data/*.db
clone/data/*.db-*
*.db
*.db-shm
*.db-wal

# Visual Studio auto-generates a matching .sln next to a .csproj it opens
# directly; the only real solution file is clone/TotalWeightPlc.slnx.
clone/src/**/*.sln
clone/tests/**/*.sln
